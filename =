N = int(input())
M = int(input())
start, end = 1, M
while start <= end :
    mid = (start + end) // 2
    cnt = 0
    for i in range(1, N+1) :
        cnt += min(N, mid // i)
    if cnt < M :
        start = mid + 1
    else :
        end = mid - 1
        result = mid
print(result)

